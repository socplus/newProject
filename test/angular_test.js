const mocha = require('mocha');
const assert = require('assert');

describe('Test case 1', function(){

	//Create test

	/**
	Since the Service : APPSERVICE have only one function, which is pulling the data from the JSON,
	Therefore the test cases for the functions defined in the APPCOMPONENT are not done here. The service is
	tested.
	*/
	it('test function', function(){

		assert(); 
	})




})